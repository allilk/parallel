import { videoId } from '../stores';

const loadNewVideo = (videoId) => {
	// if ($videoId.length > 0) {
	//     if (!player) player = getPlayer();
	//     player.loadVideoById(parsed).pauseVideo();
	// }
	// videoId.set(parsed.videoId);
};

export default loadNewVideo;
